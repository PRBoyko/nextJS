import React from 'react';
import styles from './logo.module.css';

function Logo(props) {
    return (
        <div className={styles.logo}>
            Logo
        </div>
    );
}

export default Logo;